apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-resources
  namespace: k8limited
spec:
  hard:
    pods: "4"
    requests.cpu: "1"
    requests.memory: 1Gi 
    limits.cpu: "2"
    limits.memory: 2Gi

